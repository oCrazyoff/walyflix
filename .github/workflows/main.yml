on:
  push:
    branches:
      - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 45.132.157.8
          username: u262084135.magenta-sandpiper-829619.hostingersite.com
          password: ${{ secrets.senha_hostinger }}
          local-dir: ./
          server-dir: ./